# FCC Process Analysis

Statistical process monitoring and time-series analysis on one year of synthetic FCC sensor data — applying NumPy, Pandas, and visualization to the kind of questions a process operator asks every shift.

---

## 📦 Dataset

This project uses the **synthetic FCC dataset** generated by [synthetic-fcc-dataset](https://github.com/gjmelief/synthetic-fcc-dataset) — 105,120 rows of process data at 5-minute intervals over one full year.

The dataset simulates realistic FCC operations including catalyst activity degradation, seasonal air flow variation, process disturbances, and PID control behavior. See that repository for full column definitions and generation methodology.

**Dataset:** `fcc_dataset_large.csv` (~50MB, not tracked in this repo)

To reproduce this analysis, generate the dataset first:
```bash
# In the synthetic-fcc-dataset repo
python generate_dataset.py
# Then copy fcc_dataset_large.csv into the data/ folder of this repo
```

---

## 🎯 Analysis Goals

This project asks the same questions a shift supervisor would ask — but answered with data instead of gut feel:

- Which process variables are most variable, and is that variability expected or concerning?
- Where are the statistical outliers, and do they cluster around known events (catalyst replacement, disturbances)?
- What does "normal operation" look like as a statistical envelope — and how often does the process run outside it?
- How do correlated variables like `Reactor_Temperature` and `Conversion_Rate` behave across time?
- How does process performance vary by shift (early / late / night)?

---

## 📊 Analysis Phases

| # | Notebook | Focus | Status |
|---|----------|-------|--------|
| 1 | `01_numpy_process_health_check.ipynb` | Descriptive stats, outlier detection with NumPy | ✅ Complete |
| 2 | `02_pandas_process_health_check.ipynb` | Filtering, descriptive stats, event impact analysis, outlier detection, ranking | ✅ Complete |
| 3 | `03_pandas_shift_performance.ipynb` | Shift comparison, groupby, variability analysis | ✅ Complete |
| 4 | `04_visualization.ipynb` | Process trend charts, control charts | ⏳ Planned |

---

## 🗂️ Repository Structure
```
fcc-process-analysis/
├── data/
│   └── fcc_dataset_large.csv        # Generated dataset (not committed — see above)
├── notebooks/
│   ├── 01_numpy_process_health_check.ipynb
│   ├── 02_pandas_process_health_check.ipynb
│   └── 03_pandas_shift_performance.ipynb
├── src/
│   └── (reusable functions — added in later phases)
├── requirements.txt
└── README.md
```

---

## 🔧 Technical Stack

- **Python** 3.x
- **NumPy** — array operations, statistical functions *(Phase 1)*
- **Pandas** — data manipulation, filtering, groupby, ranking *(Phase 2–3)*
- **Matplotlib** — visualization *(Phase 4)*

---

## 🚀 Getting Started
```bash
git clone https://github.com/gjmelief/fcc-process-analysis.git
cd fcc-process-analysis
pip install -r requirements.txt
```

Open notebooks in VS Code or run `jupyter notebook` in your terminal.

---

## 📈 Key Findings

- Descriptive and outlier analysis (NumPy/Pandas) confirmed which process variables show the most variability and where statistical outliers cluster.
- Shift performance comparison (early/late/night) found no meaningful variance between shifts — expected given the dataset's synthetic generation, but a useful sanity check on the analysis method itself.
- Visualization phase planned next to turn these findings into trend and control charts.

---

*Related project: [synthetic-fcc-dataset](https://github.com/gjmelief/synthetic-fcc-dataset)*
